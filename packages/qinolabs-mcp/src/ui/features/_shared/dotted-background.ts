/**
 * Dotted background style for non-React Flow content areas.
 * Creates a subtle grid pattern that provides visual texture.
 */
export const dottedBackgroundStyle: React.CSSProperties = {
  backgroundImage:
    "radial-gradient(circle, rgb(120 113 108 / 0.15) 1px, transparent 1px)",
  backgroundSize: "20px 20px",
};
